<div class="container">
  <div class="row">
    <div class="col-lg-8">
      <h4>
        <% if (reviews.length > 0) { %>
          <%= pluralize('Review', reviews.length, true) %>
        <% } else { %>
          No reviews
        <% } %>
      </h4>
      <div class="review-list">

      </div>
    </div>
  </div>
</div>
